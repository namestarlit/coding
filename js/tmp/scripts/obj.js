#!/usr/bin/node

/* Objects */
person = { name: 'Starlit', age: 22, gender: 'male', state: 'happy man' };

console.log(`I am ${person.name}, a ${person.age} year old ${person.gender} and I am a ${person.state}.`);
console.log('I am of type', typeof (person));
