# Why does # in print("Hi # there.") not get ignored?

print("Hi # there.")

# The # character in that code, is inside a string, so it will be put into the
# so it will be put into the string until the ending " character is hit.
# Hash / octothorpe characters in strings are just considered characters, not comments
